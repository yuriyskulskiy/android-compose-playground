RectSnakeBorder TODO

1. Consider extracting pointAt(distance) into a private helper on top of RectSnakeTrackGeometry.
2. Consider extracting snake progress/head/tail/alpha calculation into a separate helper.
3. Consider extracting colorLerp and pointOnArc into small private helpers with cleaner ownership.
4. Consider extracting glow paint creation into small private factory helpers if the main modifier
   stays noisy.
5. Revisit arc rendering and replace manual sampling with drawArc/native drawArc using sweep
   gradient.
6. Validate that the drawArc approach preserves current visual fade/head-tail behavior closely
   enough before deleting the sample-based branch.
7. Add support for asymmetric corner radii after geometry layer is stabilized.
8. Keep refactors behavior-preserving until arc rendering is changed intentionally.
